name: Compile assets

on:
  push:
    branches-ignore: [ main, master ]

  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v7

      - name: Compile LESS
        uses: hivepress/action-compile-assets/less@v1.1.0
        with:
          target: 'assets/css/*.less'

      - name: Generate RTL styles
        run: |
          for file in assets/css/*.less; do
            name="$(basename "$file" .less)"
            npx --yes rtlcss@4 "assets/css/$name.css" "assets/css/$name-rtl.css"
          done

      - name: Minify assets
        uses: hivepress/action-minify-assets@v2.1

      - name: Commit changes
        run: |
          git config --global user.name 'github-actions[bot]'
          git config --global user.email 'github-actions[bot]@users.noreply.github.com'
          git diff --quiet HEAD || (git commit -am "Compile assets" && git push)
